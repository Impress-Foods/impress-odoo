<?xml version="1.0" encoding="utf-8"?>
<?xml-model href="../../import_xml.rng" type="application/xml"?>
<odoo>

    <!-- View stock.picking form -->
    <record id="view_stock_picking_form" model="ir.ui.view">
        <field name="name">view.stock.picking.form</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="arch" type="xml">
            <xpath expr="//button[@name='do_print_picking']" position="after">
                <field name="shipping_label_attachment_id" invisible="1" />
                <button
                    name="%(delivery_common.report_shipping_label)d"
                    type="action"
                    string="Print Shipping Label"
                    invisible="not shipping_label_attachment_id"
                />
            </xpath>
        </field>
    </record>

    <record id="stock_picking_kanban" model="ir.ui.view">
        <field name="name">stock.picking.view.kanban</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.stock_picking_kanban" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='picking_properties']" position="after">
                <field name="carrier_id" />
                <span t-if="record.carrier_id.value"> &amp;nbsp;-&amp;nbsp;</span>
            </xpath>
            <xpath expr="//t[1]" position="inside">
                <field name="ready_to_ship" invisible="1" />
            </xpath>
            <xpath expr="//t/div[1]" position="attributes">
                <attribute
                    name="t-attf-class"
                >{{ record.ready_to_ship.raw_value ? 'oe_kanban_color_10' : 'oe_kanban_color_1' }} oe_kanban_card oe_kanban_global_click</attribute>
            </xpath>
        </field>
    </record>
</odoo>