<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_order_form_analytic_account" model="ir.ui.view">
        <field name="name">sale.order.form.analytic.account</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <field name="journal_id" position="after">
                <field
                    name="analytic_account_id"
                    context="{'default_partner_id': partner_invoice_id, 'default_name': name}"
                    readonly="invoice_count != 0 and state == 'sale'"
                    groups="analytic.group_analytic_accounting"
                    force_save="1"
                />
            </field>
        </field>
    </record>

    <record id="view_sales_order_filter_analytic_account" model="ir.ui.view">
        <field name="name">sale.order.list.select.analytic.account</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter" />
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field
                    name="analytic_account_id"
                    groups="analytic.group_analytic_accounting"
                />
            </field>
        </field>
    </record>
</odoo>
