<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <!-- View stock.picking form -->
    <record id="view_stock_picking_form" model="ir.ui.view">
        <field name="name">view.stock.picking.form</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id']" position="after">
                <field name="clickship_rate_needed" invisible="1" />
            </xpath>
            <xpath expr="//div[@name='tracking']" position="after">
                <field name="clickship_service_id" readonly="1" />
            </xpath>
            <xpath expr="//button[@name='button_validate'][1]" position="attributes">
                <attribute name="invisible">
                    state in ('draft', 'confirmed', 'done', 'cancel') and not clickship_rate_needed
                </attribute>
            </xpath>
            <xpath expr="//button[@name='button_validate'][2]" position="attributes">
                <attribute name="invisible">
                    state in ('waiting', 'assigned', 'done', 'cancel') and not clickship_rate_needed
                </attribute>
            </xpath>
            <xpath expr="//button[@name='button_validate'][1]" position="before">
                <button
                    name="action_get_clickship_rates"
                    type="object"
                    string="Get Rates"
                    invisible="not clickship_rate_needed or carrier_id.delivery_type == 'clickship'"
                    class="oe_highlight"
                />
            </xpath>
            <xpath expr="//button[@name='action_cancel'][1]" position="before">
                <button
                    name="action_get_clickship_rates"
                    type="object"
                    string="Get Rates"
                    invisible="clickship_rate_needed or carrier_tracking_ref  or carrier_id.delivery_type =='clickship'"
                />
            </xpath>
        </field>
    </record>

</odoo>
